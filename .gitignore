# Dependencies
node_modules/
.pnp
.pnp.js

# Library build output (regenerated by `npm run build` / prepare)
dist/
*.tsbuildinfo

# Test coverage
coverage/

# Expo / React Native (example app)
.expo/
.expo-shared/
web-build/
expo-env.d.ts
.metro-health-check*
*.hprof

# Native build output (only present after `expo prebuild` / bare workflow)
# /example/ios/
# /example/android/

# Logs
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
.pnpm-debug.*

# Environment / secrets
.env
.env.*
!.env.example

# Mobile signing / keys (never commit these)
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem
*.orig.*

# Editor / OS
.DS_Store
.idea/
*.swp
